import type React from "react"
// This layout is used for authentication pages (login, forgot password)
// It doesn't include the sidebar or header

export default function AuthLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
